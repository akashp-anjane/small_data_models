# models/my_model.yml

version: 2

models:
  - name: model_seed_file_8
    description: "A description of my model."
    columns:
      - name: id
        description: "Unique identifier for each record."
        tests:
          - unique
